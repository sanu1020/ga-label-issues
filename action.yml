name: "Label new pull requests cpy"
description: "Add a label when a new PR is opened"
author: "Sanujan"

inputs:
  gh-context:
    description: 'The GitHub token for authentication.'
    required: true

runs:
  using: "node16"
  main: "dist/index.js"

branding:
  icon: 'activity'
  color: 'green'